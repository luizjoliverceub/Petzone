import { PetDashboard } from "../components/PetDashboard"

export default function Home() {
    return (
        <main className="w-full h-full flex items-center justify-center animate-fade-in">
            <PetDashboard />
        </main>
    )
}
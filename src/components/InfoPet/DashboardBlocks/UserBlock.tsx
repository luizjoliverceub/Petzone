import { CreatePetSchema } from "@/utils/actions/AddPet";
import { Session } from "next-auth";

export function UserBlock() {
    return (
        <div className="border-2 rounded-xl p-8 flex flex-col gap-4 h-full w-full">
            
        </div>
    )
}